
import 'package:flutter/material.dart';

class ResetPinScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reset PIN'),
      ),
      body: Center(
        child: Text('Reset PIN Screen Content Here'),
      ),
    );
  }
}
